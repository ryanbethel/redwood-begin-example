{
  "private": true,
  "workspaces": [
    "api",
    "web",
    "packages/*"
  ],
  "scripts": {
    "build:nobuild": "yarn rw deploy netlify &&  npx zip-it-and-ship-it api/dist/functions begin/api/dist --archiveFormat none && cp begin/index.js begin/api/dist/functions/graphql"
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.33.3"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config"
  },
  "engines": {
    "node": ">=14",
    "yarn": ">=1.15"
  },
  "dependencies": {
    "@architect/sandbox": "^3.6.0",
    "@netlify/zip-it-and-ship-it": "^4.4.1",
    "serverless": "^2.47.0",
    "yarn": "^1.22.10"
  }
}